version: "3.8"

services:
  app-green:
    image: choehyungwon/memory-app:latest
    container_name: memory-app-green
    env_file:
      - .env
    ports:
      - "8082:8080"
    depends_on:
      - mysql
    restart: unless-stopped
