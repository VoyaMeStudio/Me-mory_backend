version: "3.8"

services:
  app-blue:
    image: choehyungwon/memory-app:latest
    container_name: memory-app-blue
    env_file:
      - .env
    ports:
      - "8081:8080"
    depends_on:
      - mysql
    restart: unless-stopped
